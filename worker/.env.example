# Railway → Variables (server-only secrets; never expose to the browser)
SUPABASE_URL=https://YOUR-PROJECT-ref.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service_role-key   # Settings → API Keys (SECRET — server only)
ANTHROPIC_API_KEY=sk-ant-...

# optional
OCR_MODEL=claude-sonnet-4-6   # or claude-opus-4-8 for max accuracy
POLL_MS=4000
